export default {
  plugins: {
    // Leave empty to let Tailwind v4 Vite plugin handle styling,
    // and to prevent Vite from searching up to parent directories
    // where a broken postcss.config.js might exist.
  },
};
